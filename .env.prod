# Seguridad
SECRET_KEY=pon-una-clave-real-aqui
DEBUG=False

# Hosts permitidos en producción (ajusta con tu dominio o IP pública)
ALLOWED_HOSTS=api.tudominio.com,tu-ip-publica

# CORS solo para el dominio del frontend en producción
CORS_ALLOWED_ORIGINS=https://frontend.tudominio.com

# Base de datos PostgreSQL en Docker o servidor
DATABASE_URL=postgres://alexander:baspeka@db:5432/api_db
CONN_MAX_AGE=60

# Redis (broker y backend para Celery y cache)
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CELERY_TASK_ALWAYS_EAGER=False

# Correo (para recuperación de contraseña, alertas, etc.)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=admin@tudominio.com
EMAIL_HOST_PASSWORD=una-app-password-segura
